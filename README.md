# Uniswap pairs tracker

To run project, execute `yarn start` it will launch web app, which, by default will be available t http://localhost:3000. Interface allows you to track reserves of a token pairs.